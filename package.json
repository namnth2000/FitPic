{
  "name": "fitpic",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "node --check fitpic-core.js && node --check i18n.js && node --check app.js",
    "test": "node --test tests/*.test.js",
    "build": "node --check fitpic-core.js && node --check i18n.js && node --check app.js"
  }
}
